## Desenvolvimento das Features de Calendário

### Calendário

- [ ] Criar o resource de `Calendars`.
- [ ] Implementar o resource de `Calendars` com os padrões da aplicação,
    utilizando a pasta `Shared` para itens em comum.
- [ ] Implementar o `RelationManager` de `Calendars` dentro do resource de `Companies`.
- [ ] Implementar o `RelationManager` de `CalendarEvents` dentro do resource de `Calendars`.

### Eventos do Calendário

- [ ] Criar o resource de `CalendarEvents`.
- [ ] Implementar o resource de `CalendarEvents` com os padrões da aplicação,
    utilizando a pasta `Shared` para itens em comum.
- [ ] Implementar o `RelationManager` de `CalendarEvents` dentro do resource de `Calendars`.
