## Desenvolvimento das Features de Auditoria

### Auditoria

- [ ] Criar o resource de `AuditLogs`.
- [ ] Implementar o resource de `AuditLogs` com os padrões da aplicação,
    utilizando a pasta `Shared` para itens em comum.
- [ ] Implementar o `RelationManager` de `AuditLogs` dentro do resource de `Companies`.
- [ ] Implementar o `RelationManager` de `AuditLogs` dentro do resource de `Users`, através de `CompanyUsers`. (ActorUser e ActorCompanyUser)
- [ ] Implementar a captura de logs de auditoria para ações importantes na aplicação,
    como criação, atualização e exclusão de registros.
