## Desenvolvimento das Features de PTO (Approvals e Requests)

### Aprovações de PTO

- [ ] Criar o resource de `PtoApprovals`.
- [ ] Implementar o resource de `PtoApprovals` com os padrões da aplicação,
    utilizando a pasta `Shared` para itens em comum.
- [ ] Mostrar, baseado em permissões, as aprovações pendentes para o usuário logado.
- [ ] Mostrar o item de menu de `PTO Approvals` apenas para usuários com permissão adequada.

### Solicitações de PTO

- [ ] Criar o resource de `PtoRequests`.
- [ ] Implementar o resource de `PtoRequests` com os padrões da aplicação,
    utilizando a pasta `Shared` para itens em comum.
