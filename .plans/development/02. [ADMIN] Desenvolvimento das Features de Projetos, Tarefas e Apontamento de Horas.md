## Desenvolvimento das Features de Projetos, Tarefas e Apontamento de Horas

### Projetos

- [x] Criar o resource de `Projects`.
- [x] Implementar o resource de `Projects` com os padrões da aplicação,
    utilizando a pasta `Shared` para itens em comum.
- [x] Implementar o `RelationManager` de `CompanyProjects` dentro do resource de `Companies`.

### Tarefas

- [x] Criar o resource de `Tasks`.
- [x] Implementar o resource de `Tasks` com os padrões da aplicação,
    utilizando a pasta `Shared` para itens em comum.
- [x] Implementar o `RelationManager` de `ProjectTasks` dentro do resource de `Projects`.

### Atribuições

- [x] Implementar o `RelationManager` de `Users` dentro do resource de `Projects` através de `Assignments`.
- [ ] Implementar o `RelationManager` de `Projects` dentro do resource de `Users` através de `Assignments`.
- [ ] Utilizar a pasta `Shared` para itens em comum.

### Apontamento de Horas

- [ ] Implementar o `RelationManager` de `TimeEntries` dentro dos resources de `Tasks`, `Projects` e `Users`
    através de `TimeEntries`.
- [ ] Utilizar a pasta `Shared` para itens em comum.
