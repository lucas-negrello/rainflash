## Desenvolvimento das Features de Horários e Valores

### Horários de Trabalho

- [ ] Criar o resource de `WorkSchedules`.
- [ ] Implementar o resource de `WorkSchedules` com os padrões da aplicação,
    utilizando a pasta `Shared` para itens em comum.
- [ ] Implementar o `RelationManager` de `WorkSchedules` dentro do resource de `Users`, relacionando com `CompanyUser`.

### Valores e Tarifas

- [ ] Criar o resource de `RateHisories`.
- [ ] Implementar o resource de `RateHistories` com os padrões da aplicação,
    utilizando a pasta `Shared` para itens em comum.
- [ ] Implementar o `RelationManager` de `RateHistories` dentro do resource de `Users`, relacionando com `CompanyUser`.
